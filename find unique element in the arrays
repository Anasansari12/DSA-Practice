#include <bits/stdc++.h>
using namespace std;
class solution{
  public:
  void f1(vector<int>arr){
      int flag=false,ans=-1;
      for(int i=0;i<arr.size();i++){
          flag=false;
          for(int j=0;j<arr.size();j++){
              if( i!=j && arr[i]==arr[j]){
                  flag=true;
              }
          }
          if(flag==false)ans=arr[i];
      }cout<<ans;
  }
  void f2(vector<int>arr){
       unordered_map<int,int>map;
       for(int i=0;i<arr.size();i++)map[arr[i]]++;
       for(int i=0;i<map.size();i++){
           if(map[arr[i]]==1){
               cout<<arr[i];
           }
       }
  }
  
};
int main() {
vector<int>arr={1,2,1,2,3,5,4,5,4};
solution s;
s.f1(arr);
s.f2(arr);

}
